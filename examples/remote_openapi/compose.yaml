name: genro-opaque-docker-poc
services:
  app:
    image: genro-opaque-openapi:issue72
    build:
      context: ../..
      dockerfile: examples/remote_openapi/Dockerfile
    ports:
      - "127.0.0.1:${REMOTE_PORT:-18765}:8765"
    stop_grace_period: 8s
