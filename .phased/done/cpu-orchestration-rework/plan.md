# Context: wf/cpu-orchestration-rework
Parent: feat/cpu-orchestration
Mode: interactive
Channel: in-chat
Must not break: `WorkerHandler.record_cpu_reading` stays the ONLY writer of `cpu_temperature_percent`; `get_cpu_temperature_percent` stays the ONLY reader the judges use — the photo's `cpu_percent` gains no reader
Must not break: every journal reason code that survives this plan keeps its spelling; the next plan (tests reclassified, the photo's CPU smoothing removed) greps for them
Must not break: `freeze_hosted_user`, the hold barrier, the fold and the data plane are not touched

## Objective
The CPU decides, the memory is a final veto, a worker is born only when nobody admits the user (owner rule, 2026-09-01; decisions 2, 3, 3b, 5, 6, 7 of `temp/scheda_ragioni_cpu_orchestration_2026-09-01.md`). The 100 ms temperature is consumed through an asymmetric first-order filter; the worker gate and the placement read the filtered temperature, the memory only refuses; the user cost estimate disappears with the gate that read it; the retirement is judged on temperature with a memory veto. Measured defect this plan answers: on run `temp4` the admission flipped 1429 times and 52 of 85 offloaded users returned to the same worker within 0.3 s.

## Work Plan
- [x] **Phase 1**: The asymmetric filter on the worker temperature
  > Done: `record_cpu_reading` keeps the raw share in `cpu_temperature_sample_percent` and moves `cpu_temperature_percent` through the asymmetric first-order filter (`cpu_heating_seconds` 1.0 up, `cpu_cooling_seconds` 5.0 down, first sample seeds); both are group setpoints in policy, grammar and handler lists; the census carries raw and filtered; `commander_kwargs` reads `cpu_temperature_sample_seconds`. Full suite 1784 passed, `ruff check src/ tests/` clean.
  > Files: src/genro_asgi/spa/orchestration/worker_handler.py, src/genro_asgi/spa/orchestration/group_policy.py, src/genro_asgi/spa/orchestration/group_handler.py, src/genro_asgi/spa/orchestration/spa_commander.py, src/genro_asgi/applications/spa_app.py, src/genro_asgi/config/handler.py, docs/guides/configuration.md, internals/10_server/020_applications/configuration_profiles/README.md, CLAUDE.md, tests/orchestration/test_orchestration_cpu_temperature_meter.py, tests/orchestration/test_orchestration_census.py, tests/test_group_policy.py, tests/test_config.py, .phased/active/cpu-orchestration-rework/notes.md
  > Review: the `Done:` grep names the commander census, which carries values and not the setpoint name — read as recorded in notes.md. One existing meter test changed meaning by design (one idle sample no longer reopens a hot worker).
  - Run: opus / medium
  - Pattern: `src/genro_asgi/spa/orchestration/worker_handler.py:record_cpu_reading` (the writer being extended), `src/genro_asgi/spa/orchestration/group_policy.py:_check_value` (how a setpoint is declared and validated), `tests/orchestration/test_orchestration_cpu_temperature_meter.py:test_two_readings_make_separate_temperature_telemetry` (the test style)
  - Files: `src/genro_asgi/spa/orchestration/worker_handler.py`, `src/genro_asgi/spa/orchestration/group_policy.py`, `src/genro_asgi/spa/orchestration/group_handler.py` (two properties), `src/genro_asgi/spa/orchestration/spa_commander.py` (census rows), `src/genro_asgi/applications/spa_app.py` (grammar), `src/genro_asgi/config/handler.py` (group key list AND `commander_kwargs`), `docs/guides/configuration.md`, `internals/10_server/020_applications/configuration_profiles/README.md`, `CLAUDE.md`, `tests/orchestration/test_orchestration_cpu_temperature_meter.py`, `tests/test_group_policy.py`, `tests/test_config.py`
  - Decisions:
    - Two group setpoints: `cpu_heating_seconds` (default 1.0) and `cpu_cooling_seconds` (default 5.0), both `> 0` (low bound exclusive), not nullable. `GroupHandler` exposes them as properties like the others.
    - In `record_cpu_reading`, after the raw share `sample` is computed: `cpu_temperature_sample_percent = sample` (raw, telemetry only); `tau = cpu_heating_seconds if sample > current else cpu_cooling_seconds`; `alpha = 1 - exp(-elapsed / tau)`; `cpu_temperature_percent = current + alpha * (sample - current)`. The first sample of a process seeds the filter (`cpu_temperature_percent = sample`). A reading that clears the measure (None, changed birth) clears both values. `get_cpu_temperature_percent` is untouched: it already returns `cpu_temperature_percent`, so the three judges and the tests that write that attribute directly (`declare_cpu`) keep working.
    - The handler reads the two `tau` off `self.group_handler.policy` at each sample; no copy on the handler.
    - Census: worker rows gain `cpu_temperature_sample_percent` beside `cpu_temperature_percent`; `inspector.html` is not touched here (Phase 2 reshapes it).
    - `config/handler.py:commander_kwargs` gains `"cpu_temperature_sample_seconds"` in its `closed_attrs` list — the grammar already declares it on `commander` and `SpaCommander.__init__` already takes it; today a recipe declaring it is silently ignored (found by the previous plan, phase 1).
    - Docs: `configuration.md` gets one paragraph on the filter (what the two seconds mean, that the raw 100 ms sample is telemetry only); the profiles README example adds the two keys; `CLAUDE.md` paragraph "Worker CPU temperature is a separate measurement channel" says the judges read the filtered value and names the two setpoints; `Last Updated` follows.
  - Details: setpoints first (policy, grammar, handler key lists, properties), then the filter with its tests (heating faster than cooling; a 0% sample on a hot worker lowers the filtered value by less than `alpha_cooling`; first sample seeds; clearing clears both; the census carries both), then `commander_kwargs` with a config test that a recipe declaring `cpu_temperature_sample_seconds` reaches the commander, then docs.
  - Done: `pytest tests/orchestration/test_orchestration_cpu_temperature_meter.py tests/test_group_policy.py tests/test_config.py -q` passes; `pytest tests/ -q` green; `ruff check src/ tests/` zero errors; `git grep -c cpu_heating_seconds -- src docs internals CLAUDE.md` lists policy, grammar, handler, commander census, the three docs.

- [x] **Phase 2**: The gate and the placement read the CPU; the memory refuses; the estimate goes
  > Done: `WorkerHandler.assign_user(user)` judges state, heads and the memory veto `worker_memory_admission_percent` (80, replaces `occupancy_max_percent`); `_placement_candidate` walks the CPU-open workers hottest-first, skips a worker inside `worker_admission_interval_seconds` (1.0) and takes the hottest that admits when all are in their window; the estimate chain is gone (`get_occupancy_percent`, `_expected_occupancy`, `new_user_occupancy_percent`, the user row's `occupancy_percent`, the fold's stamp, `mark_user_frozen(user)`); census and inspector show memory and temperature. Journal: `hottest_cpu_open_candidate`, `worker_recently_admitted`, `worker_memory_full`, `all_workers_recently_admitted`. Full suite 1786 passed, `ruff check src/ tests/` clean.
  > Files: src/genro_asgi/spa/orchestration/worker_handler.py, src/genro_asgi/spa/orchestration/group_handler.py, src/genro_asgi/spa/orchestration/group_policy.py, src/genro_asgi/spa/orchestration/spa_commander.py, src/genro_asgi/spa/orchestration/envelope_handler.py, src/genro_asgi/applications/spa_app.py, src/genro_asgi/config/handler.py, src/genro_asgi/applications/server_sections/resources/inspector.html, pyproject.toml, docs/guides/configuration.md, internals/10_server/020_applications/configuration_profiles/README.md, CLAUDE.md, tests/orchestration/test_orchestration_placement_interval.py (new), tests/orchestration/group_stub.py, tests/orchestration/test_orchestration_placement.py, tests/orchestration/test_orchestration_cpu_growth.py, tests/orchestration/test_orchestration_group_handler.py, tests/orchestration/test_orchestration_envelope_chain.py, tests/orchestration/test_orchestration_spa_commander.py, tests/orchestration/test_orchestration_request_chain.py, tests/orchestration/test_orchestration_heartbeat.py, tests/orchestration/test_orchestration_census.py, tests/orchestration/test_orchestration_pss_accounting.py, tests/orchestration/test_orchestration_memory_headroom.py, tests/orchestration/test_orchestration_m2_e2e.py, tests/orchestration/test_orchestration_m3_e2e.py, tests/orchestration/test_orchestration_m4_e2e.py, tests/orchestration/test_orchestration_no_reception_reserve.py, tests/orchestration/test_orchestration_no_speculative_birth.py, tests/orchestration/test_orchestration_policy_delegation.py, tests/orchestration/test_orchestration_apply.py, tests/test_group_policy.py, tests/test_config.py, tests/test_spa_app_profiles.py, tests/test_spa_profile_grammar.py, tests/test_apply_group_settings.py, tests/test_configuration_profiles_application.py, .phased/active/cpu-orchestration-rework/notes.md
  > Review: the worker gate does not refuse a CPU-closed worker (Decisions said so; the fallback needs the gate to admit them) — recorded in notes.md. The journal code `fullest_cpu_open_candidate` became `hottest_cpu_open_candidate`. Three tests re-based on the veto (79→85%, m4 concession 26→24 MB); two tests of the deleted estimate removed.
  - Run: opus / high
  > Batches: 1 setpoints and the worker gate | 2 placement order and the admission interval | 3 the estimate removed, census and inspector reshaped
  - Pattern: `src/genro_asgi/spa/orchestration/worker_handler.py:assign_user` (the gate being rewritten), `src/genro_asgi/spa/orchestration/group_handler.py:_placement_candidate` (the ordering being rewritten), `src/genro_asgi/spa/orchestration/group_handler.py:_judge_cpu_admission` (how a decision is journaled with `numbers` and `candidates`)
  - Files: `src/genro_asgi/spa/orchestration/worker_handler.py`, `src/genro_asgi/spa/orchestration/group_handler.py`, `src/genro_asgi/spa/orchestration/group_policy.py`, `src/genro_asgi/spa/orchestration/spa_commander.py`, `src/genro_asgi/spa/orchestration/envelope_handler.py`, `src/genro_asgi/applications/spa_app.py`, `src/genro_asgi/config/handler.py`, `src/genro_asgi/applications/server_sections/resources/inspector.html`, `docs/guides/configuration.md`, `internals/10_server/020_applications/configuration_profiles/README.md`, `CLAUDE.md`, and the tests that name what changes (`git grep -lE 'occupancy_max_percent|new_user_occupancy_percent|get_occupancy_percent|_expected_occupancy|\["occupancy_percent"\]|mark_user_frozen|worker_cap' -- tests`)
  - Decisions:
    - `worker_memory_admission_percent` (default 80, `0 <= x <= 100`) REPLACES `occupancy_max_percent`; cross rule `worker_memory_admission_percent < restart_occupancy_max_percent`. The old cross rule `close_occupancy_max_percent < occupancy_max_percent` is deleted (Phase 3 replaces the close setpoint).
    - `worker_admission_interval_seconds` (default 1.0, `>= 0`; 0 = off). `WorkerHandler` keeps `last_admission_monotonic` (born None), written by `GroupHandler.assign_user` when a placement lands on that worker.
    - `WorkerHandler.assign_user(user)` — the `occupancy_percent` parameter goes. Order of the checks: state (`WorkerQuittingError` / `AssignmentRefused` as today), `worker_max_users` (`NoRoomError`), `cpu_admission_open` False → `AssignmentRefused`, memory: `get_memory_occupancy_percent(worker_snapshot) > worker_memory_admission_percent` → `NoRoomError`. The interval is NOT judged here: it is the placement's ordering rule, not a refusal (a worker in its window still takes the user when every open worker is in its window).
    - `_placement_candidate`: candidates = living workers with `cpu_admission_open`, sorted by `get_cpu_temperature_percent() or 0.0` descending. First pass: skip a candidate whose `last_admission_monotonic` is younger than the interval; the first that admits wins. Second pass (only when every candidate was skipped): same order, interval ignored, the first that admits wins, journaled with reason `all_workers_recently_admitted`. The per-candidate decision row gains `recently_admitted: bool`; a skipped candidate is journaled inside the row as `worker_recently_admitted`. A memory refusal is journaled on the row as `worker_memory_full` (the refusal text stays too).
    - `_fallback_candidate`: the CPU-closed workers, hottest first, each through the same `assign_user(user)` — the memory veto is what still stands there.
    - The estimate goes: `GroupHandler.get_occupancy_percent`, `_expected_occupancy`, the setpoint `new_user_occupancy_percent`, `user_map[...]["occupancy_percent"]` (from `_new_row` too), the `occupancy_percent` parameter of `SpaCommander.mark_user_frozen` (`on_user_frozen` and `on_process_quitted` in the fold call it with the user only), the estimate stamp in `WorkerEnvelopeHandler.work_on_envelope` (the method reduces to `super()`; delete the override if nothing else is left), `expected_occupancy_percent` in journal `numbers`. Old reboot pickles carrying the key are read as they are (`adopt_frozen_registers` takes the rows whole); no migration.
    - Census: worker rows lose `occupancy_percent` and `worker_cap`, keep `memory_occupancy_percent`, `cpu_temperature_percent`, `cpu_temperature_sample_percent`; user rows lose `occupancy_percent`. `inspector.html:166` shows `state · mem NN% · cpu NN%`.
    - `check_occupancy` and `_grow`-less picture: the `occupancy_reader` branch disappears; `picture` for the retirement stays MEMORY in this phase (Phase 3 switches it), so `_spare_worker` keeps working on `close_occupancy_max_percent` until then.
    - Docs: `configuration.md` "The occupancy keys" paragraph becomes "The memory veto" (one key: `worker_memory_admission_percent`, plus the restart one) and the CPU paragraph gains the interval; profiles README example replaces `occupancy_max_percent`; `CLAUDE.md` paragraph "CPU pressure gates admission" is rewritten: filtered temperature, hottest-open-first, the interval, the memory veto, no estimate.
  - Details: batch 1 — policy + grammar + handler list + the new `assign_user` with its tests; batch 2 — `_placement_candidate` / `_fallback_candidate` / `assign_user` (group) writing `last_admission_monotonic`, journal rows, tests for the order, the interval and the all-in-window rule; batch 3 — deletions listed above, census, inspector, docs, test adaptations. Every batch ends with `ruff check src/ tests/` and the full suite.
  - Done: `git grep -nE 'occupancy_max_percent|new_user_occupancy_percent|get_occupancy_percent|_expected_occupancy|expected_occupancy_percent|worker_cap' -- src docs internals CLAUDE.md` prints nothing; `git grep -n '"occupancy_percent"' -- src` prints nothing; `pytest tests/ -q` green; `ruff check src/ tests/` zero errors; the pool census of a running group carries `memory_occupancy_percent`, `cpu_temperature_percent` and `cpu_temperature_sample_percent` per worker and no `occupancy_percent` (asserted by a test).
  - Verify: now — read the rewritten `CLAUDE.md` paragraph and the three journal reason codes in one placement of a warm group (`temp/` recipe or the m3 story): the numbers in `candidates` must let you rebuild the choice.

- [x] **Phase 3**: The retirement is judged on temperature, the memory vetoes
  > Done: `cpu_close_percent` (nullable, `null` = the reopen threshold; explicit `<= cpu_admission_reopen_percent` when the admission policy is on) replaces `close_occupancy_max_percent`; `_spare_worker(policy)` needs a temperature on every living worker (else `cpu_temperature_missing`, nothing closes), takes the coldest, shares its heat onto the survivors under the threshold, then the memory veto per survivor, then the heads; `check_occupancy` keeps the memory picture for the restart only. Full suite 1788 passed, `ruff check src/ tests/` clean.
  > Files: src/genro_asgi/spa/orchestration/group_handler.py, src/genro_asgi/spa/orchestration/group_policy.py, src/genro_asgi/applications/spa_app.py, src/genro_asgi/config/handler.py, docs/guides/configuration.md, CLAUDE.md, tests/orchestration/test_orchestration_group_handler.py, tests/orchestration/test_orchestration_cpu_growth.py, tests/orchestration/test_orchestration_m3_e2e.py, tests/orchestration/test_orchestration_policy_delegation.py, tests/orchestration/test_orchestration_apply.py, tests/test_group_policy.py, tests/test_apply_group_settings.py, tests/test_spa_app_profiles.py, .phased/active/cpu-orchestration-rework/notes.md
  > Review: `cpu_close_percent` is nullable with the reopen threshold as its meaning when unset (owner, option c) — the Decisions said default 40 with the rule always on, which refused every profile lowering the reopen threshold. Closure tests re-based on temperatures; the m3 story samples its processes for real.
  > Verify: deferred: needs the bench in genropy-asgi — a run on the `temp4` plan with the new setpoints; acceptance on the journal: share of the busiest worker at 160 active, number of offloads that returned to the source worker.
  - Run: opus / medium
  - Pattern: `src/genro_asgi/spa/orchestration/group_handler.py:_spare_worker` (the judge being rewritten), `tests/orchestration/test_orchestration_cpu_growth.py:test_after_the_descent_and_the_quiet_the_spare_worker_is_released` (the retirement test style with `group_clock`)
  - Files: `src/genro_asgi/spa/orchestration/group_handler.py`, `src/genro_asgi/spa/orchestration/group_policy.py`, `src/genro_asgi/applications/spa_app.py`, `src/genro_asgi/config/handler.py`, `docs/guides/configuration.md`, `internals/10_server/020_applications/configuration_profiles/README.md`, `CLAUDE.md`, and the tests that name `close_occupancy_max_percent` or exercise `_spare_worker` (`git grep -lE 'close_occupancy_max_percent|spare' -- tests`)
  - Decisions:
    - `cpu_close_percent` (default 40, `0 <= x <= 100`) REPLACES `close_occupancy_max_percent`; cross rule: when `cpu_admission_close_percent` is set, `cpu_close_percent <= cpu_admission_reopen_percent` (a closure must never reopen the growth cycle).
    - `_spare_worker(policy)` reads no picture argument: candidates as today (not the reception, not quitting, older than `worker_min_life_seconds`); every living worker must have a temperature (`get_cpu_temperature_percent()` not None) — otherwise NO retirement this round, journaled once per round as `retirement` / `no_action` with reason `cpu_temperature_missing` and the worker names. Spare = the coldest candidate. Simulation: `shared = spare temperature / len(remaining)`; every survivor's `temperature + shared <= cpu_close_percent`. Memory veto: every survivor's `memory_occupancy_percent + spare memory / len(remaining) <= worker_memory_admission_percent`. Heads: unchanged.
    - `check_occupancy`: the restart check keeps its memory picture; the retirement call becomes `_spare_worker(policy)`; the `picture` dict and its `numbers={"workers": len(picture)}` become `len(self.living_workers)`.
    - The suspension (`get_retirement_suspension`) is unchanged.
    - Docs: `configuration.md` retirement paragraph on `cpu_close_percent` and the memory veto; profiles README; `CLAUDE.md` paragraph "The retirement holds through CPU pressure" gains one sentence on the temperature judgment and the veto; the paragraph at `group_handler.py:87-95` (module docstring) is rewritten; `record_cpu_pressure`'s "rearm30" becomes "reopen at 30".
  - Details: setpoint and cross rule with tests; `_spare_worker` rewrite with tests (coldest is the spare; a survivor pushed over `cpu_close_percent` blocks; a survivor pushed over the memory veto blocks; a missing temperature blocks with the journal row; heads unchanged); `check_occupancy` wiring; docs.
  - Done: `git grep -n close_occupancy_max_percent -- src tests docs internals CLAUDE.md` prints nothing; `pytest tests/orchestration/test_orchestration_cpu_growth.py tests/orchestration/test_orchestration_group_handler.py tests/test_group_policy.py -q` passes; `pytest tests/ -q` green; `ruff check src/ tests/` zero errors.
  - Verify: deferred: needs the bench in genropy-asgi — a run on the `temp4` plan with the new setpoints; the acceptance is the concentration (share of the busiest worker at 160 active) and the number of returned offloads, both from the journal.

## Notes
- Decisions and their reasons: `temp/scheda_ragioni_cpu_orchestration_2026-09-01.md` (untracked, this worktree). Owner's answers at planning (2026-09-02): no contract tests in the plan; journal codes `worker_recently_admitted` / `all_workers_recently_admitted` / `worker_memory_full`; a missing temperature means no retirement.
- The interval is an ordering rule, never a refusal and never a birth: a group whose open workers are all in their window still places on the hottest that admits.
- Out of scope, deliberately: memory through psutil; the photo's `cpu_percent` smoothing (`CPU_SMOOTHING_FACTOR`, no reader left) and the test reclassification — next plan, autonomous.
- The bench that measures the outcome lives in genropy-asgi (`benchmarks/cpu_offload/`), not here.

## Quality check
> Quality check: 2026-09-02T05:47:35Z — commit 991ad2d — review agent, QA done (Phase 2 verify now; Phase 3 verify deferred to the genropy-asgi bench), findings 11 confirmed (3 fixed at 991ad2d, 6 docs aligned, 2 recorded), 0 dismissed
