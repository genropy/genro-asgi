# Context: wf/cpu-orchestration-cleanup
Parent: feat/cpu-orchestration
Mode: autonomous
Channel: relayed
Must not break: `WorkerHandler.record_cpu_reading(reading, *, sampled_at)` keeps its signature and its `(birth, cpu seconds, sample instant)` anchor — the filter of the next plan is written inside it
Must not break: `WorkerHandler.get_cpu_temperature_percent()` stays the ONLY reader the judges use for CPU; the photo's `cpu_percent` is never an orchestration input
Must not break: `GroupHandler.assign_user` keeps its three levels — CPU-open candidates, birth under `_placement_lock`, fallback on a CPU-closed worker under the hard cap — and `_may_grow` stays the memory veto on the birth
Must not break: `new_user_occupancy_percent`, `occupancy_max_percent`, `close_occupancy_max_percent` and `_expected_occupancy` stay: the next plan removes them together with the gate that reads them
Must not break: the journal (`log_order` / `log_decision`) keeps every existing reason code except the ones this plan deletes with their mechanism (`no_absorbable_spare_worker` stays, the reserve-related numbers go)

## Objective
Prepare the CPU orchestration rework on `feat/cpu-orchestration`: one CPU source on every platform (psutil), setpoint names that say what they do, and no mechanism left that births a worker before a user asks for it or treats the reception as a special case. Every phase removes readers before it removes the thing they read; the decisive rework (filter, gate, retirement) is the next plan and is NOT in scope here.

## Work Plan
- [x] **Phase 1**: Read the worker CPU clock through psutil on every platform
  > Done: `get_process_cpu_reading` reads `(create_time, cpu seconds)` through a per-pid cached `psutil.Process` probe; the `/proc/<pid>/stat` parser and its two constants are gone; `psutil>=6.0` is a manifest dependency.
  > Files: pyproject.toml, src/genro_asgi/spa/orchestration/worker_handler.py, tests/orchestration/test_orchestration_cpu_meter_psutil.py, tests/orchestration/test_orchestration_cpu_temperature_meter.py, tests/orchestration/test_orchestration_m4_e2e.py, CLAUDE.md, .phased/active/cpu-orchestration-cleanup/notes.md
  > Review: `cpu_temperature_sample_seconds` is a grammar kwarg on `commander` that `config/handler.py:commander_kwargs` never reads, so a recipe declaring it is silently ignored. Found while fixing the m4 e2e stories; left as found.
  - Pattern reference: `src/genro_asgi/spa/orchestration/worker_handler.py:get_process_cpu_reading` (the function being replaced; keep its contract), `tests/orchestration/test_orchestration_cpu_temperature_meter.py:test_a_reused_pid_starts_a_new_measurement_instead_of_inventing_cpu` (the test style for the meter)
  - Files: `pyproject.toml`, `src/genro_asgi/spa/orchestration/worker_handler.py`, `tests/orchestration/test_orchestration_cpu_temperature_meter.py`, `CLAUDE.md`
  - Decisions:
    - Dependency: add `"psutil>=6.0"` to `[project].dependencies` in `pyproject.toml`. Install ONLY through the manifest (`pip install -e .` from the repo root); never `pip install psutil` by name. psutil 7.1.0 is already present in the interpreter the tests run under.
    - `get_process_cpu_reading()` returns `(create_time, cpu_seconds)`: `create_time` is `psutil.Process.create_time()` (a float, replaces the `start_ticks` int as the pid-reuse witness), `cpu_seconds` is `cpu_times().user + cpu_times().system`. Returns `None` when `self.process is None` or when psutil raises `psutil.NoSuchProcess`, `psutil.AccessDenied` or `psutil.ZombieProcess`. No other exception is caught.
    - The `psutil.Process` object is built once per pid and kept on the handler as `self._process_probe` (a `psutil.Process | None`, born `None`); it is rebuilt when `self.process.pid` differs from `self._process_probe.pid`. Reason: `Process()` costs 9 µs, `cpu_times()` on a cached one 1.5 µs (measured 2026-09-01).
    - Delete `PROCESS_STAT_ROOT`, `PROCESS_CLOCK_TICKS` and the `/proc/<pid>/stat` parser. Delete the `Path` import if nothing else in the module uses it. `record_cpu_reading` is untouched except for the type of the birth element in `_cpu_meter_reading` (`float` instead of `int`).
    - `import psutil` at the top of `worker_handler.py`, in the third-party block, alphabetical. No import inside a function.
    - Module docstring of `worker_handler.py`: the paragraph "No worker-owned counters here" says "cumulative kernel clock through the handler"; keep it, and where it says the process table or Linux, say psutil. `CLAUDE.md` paragraph "Worker CPU temperature is a separate measurement channel": replace "each Linux worker's cumulative process CPU clock" with "each worker's cumulative process CPU clock through psutil", drop "Linux". `Last Updated` in `CLAUDE.md` becomes the commit date.
    - Tests: `test_the_kernel_clock_is_read_by_pid_even_when_comm_has_parentheses` is replaced by the plan's contract test for this phase (the `/proc` fixture `write_process_stat` goes with it). Every other test in the file stays; the ones that monkeypatch `get_process_cpu_reading` keep working because the contract of that method is unchanged.
  - Details:
    1. Copy `.phased/active/cpu-orchestration-cleanup/tests/phase-1/test_orchestration_cpu_meter_psutil.py` to `tests/orchestration/test_orchestration_cpu_meter_psutil.py` verbatim.
    2. Edit `pyproject.toml`, run `pip install -e .` from the repo root.
    3. Rewrite `get_process_cpu_reading` and add `_process_probe` to `__init__`; delete the two constants and the parser.
    4. Delete `test_the_kernel_clock_is_read_by_pid_even_when_comm_has_parentheses` and `write_process_stat` from the meter test module; fix the module docstring of the test file where it says "kernel row".
    5. Update the two docstrings and `CLAUDE.md` as decided.
    6. `ruff check src/ tests/`, then the two test modules, then the full suite.
  - Done: `pytest tests/orchestration/test_orchestration_cpu_meter_psutil.py tests/orchestration/test_orchestration_cpu_temperature_meter.py -q` passes; the in-tree copy of the phase-1 contract test is byte-identical to the plan copy; `git grep -nE 'PROCESS_STAT_ROOT|PROCESS_CLOCK_TICKS|/proc' -- src/genro_asgi/spa/orchestration/worker_handler.py` prints nothing; `git grep -n psutil -- pyproject.toml` prints one line; `ruff check src/ tests/` zero errors; `pytest tests/ -q` has no failure that is not also failing on the parent commit (baseline attribution: `git stash`-free, compare against `git log -1 --format=%H feat/cpu-orchestration`).

- [x] **Phase 2**: Rename the CPU admission thresholds to what they do
  > Done: `cpu_grow_percent`/`cpu_grow_rearm_percent` are gone from src, tests, docs, internals and CLAUDE.md; `cpu_admission_close_percent`/`cpu_admission_reopen_percent` carry the same defaults, bounds and cross rules, and the journal says `cpu_over_close_threshold`/`cpu_below_reopen_threshold`. Full suite 1778 passed, `ruff check src/ tests/` clean.
  > Files: src/genro_asgi/spa/orchestration/group_policy.py, src/genro_asgi/spa/orchestration/group_handler.py, src/genro_asgi/spa/orchestration/spa_commander.py, src/genro_asgi/spa/orchestration/worker_handler.py, src/genro_asgi/applications/spa_app.py, src/genro_asgi/config/handler.py, docs/guides/configuration.md, internals/10_server/020_applications/configuration_profiles/README.md, CLAUDE.md, tests/test_group_policy_admission_names.py, tests/test_group_policy.py, tests/test_configuration_profiles_application.py, tests/orchestration/group_stub.py, tests/orchestration/test_orchestration_apply.py, tests/orchestration/test_orchestration_cpu_growth.py, tests/orchestration/test_orchestration_cpu_meter_psutil.py, tests/orchestration/test_orchestration_cpu_offload.py, tests/orchestration/test_orchestration_cpu_temperature_meter.py, tests/orchestration/test_orchestration_memory_headroom.py, tests/orchestration/test_orchestration_policy_delegation.py
  > Review: `test_orchestration_cpu_growth.py:132` asserted `"cpu_grow" not in row`, which the rename made unfalsifiable; it now asserts `"order=grow" not in row`, the fact the test was written for. The phase-1 in-tree contract test no longer matches its plan copy: it carried `cpu_grow_percent` with the comment "the name before Phase 2".
  - Pattern reference: `library-standard` (a mechanical rename across a fixed file set)
  - Files: `src/genro_asgi/spa/orchestration/group_policy.py`, `src/genro_asgi/spa/orchestration/group_handler.py`, `src/genro_asgi/spa/orchestration/spa_commander.py`, `src/genro_asgi/spa/orchestration/worker_handler.py`, `src/genro_asgi/applications/spa_app.py`, `src/genro_asgi/config/handler.py`, `docs/guides/configuration.md`, `internals/10_server/020_applications/configuration_profiles/README.md`, `CLAUDE.md`, and every file under `tests/` that names the two keys (`git grep -l 'cpu_grow_' -- tests` lists them; 11 files on the parent commit)
  - Decisions:
    - `cpu_grow_percent` → `cpu_admission_close_percent`; `cpu_grow_rearm_percent` → `cpu_admission_reopen_percent`. Same defaults (None and 40.0), same bounds, same cross rules, same nullability. The `GroupHandler` properties, the grammar kwargs in `SpaApplicationGrammar`, the key list in `config/handler.py`, the journal `numbers` keys (`cpu_grow_percent`, `cpu_grow_rearm_percent` inside `_judge_cpu_admission` and `check_cpu_offload`) and every docstring follow the rename.
    - No alias, no deprecation path: an old key in a profile is refused as `unknown setpoint` by the existing `from_settings` loop. Nothing to add for that.
    - Prose that says "grow"/"growth" about this threshold (docstrings of `_judge_cpu_admission`, `record_cpu_pressure`, the `outcome` strings `"blocked: over the growth threshold"` and the reason codes `cpu_over_growth_threshold` / `cpu_below_rearm_threshold`) is rewritten to "close"/"reopen": reason codes become `cpu_over_close_threshold` and `cpu_below_reopen_threshold`, outcomes `"blocked: over the close threshold"` and `"reopened: below the reopen threshold"`. Tests asserting the old strings follow.
    - `docs/guides/configuration.md` lines 329-330, 383-403 and `internals/.../configuration_profiles/README.md` lines 62-63 carry the new names; the ordering rule reads `cpu_admission_reopen_percent < cpu_admission_close_percent < cpu_offload_percent <= 100`.
  - Details:
    1. Copy `.phased/active/cpu-orchestration-cleanup/tests/phase-2/test_group_policy_admission_names.py` to `tests/test_group_policy_admission_names.py` verbatim.
    2. `git grep -n 'cpu_grow_' -- src tests docs internals CLAUDE.md` for the full list, then rename by search-and-replace in that order: `cpu_grow_rearm_percent` FIRST (it contains the other's prefix), then `cpu_grow_percent`.
    3. Rewrite the reason codes, outcomes and docstrings listed in Decisions.
    4. `ruff check src/ tests/`, `pytest tests/ -q`.
  - Done: `git grep -c 'cpu_grow_' -- src tests docs internals CLAUDE.md` prints nothing; `git grep -c 'growth threshold' -- src` prints nothing; `pytest tests/test_group_policy_admission_names.py tests/test_group_policy.py tests/orchestration/test_orchestration_cpu_growth.py -q` passes; the in-tree copy of the phase-2 contract test is byte-identical to the plan copy; `ruff check src/ tests/` zero errors; `pytest tests/ -q` has no failure that is not also failing on the parent commit.

- [x] **Phase 3**: A worker is born only for a user who is already there
  > Done: `_has_room`, `_placeable_newcomers`, `_grow` and the setpoint `newcomer_reserve_count` are gone; `check_occupancy` births only the reception of a group with no living worker; the saturation is written by the refused `assign_user` and lifted by the next check. Full suite 1780 passed, `ruff check src/ tests/` clean.
  > Files: src/genro_asgi/spa/orchestration/group_handler.py, src/genro_asgi/spa/orchestration/group_policy.py, src/genro_asgi/applications/spa_app.py, src/genro_asgi/config/handler.py, pyproject.toml, docs/guides/configuration.md, CLAUDE.md, tests/orchestration/test_orchestration_no_speculative_birth.py, tests/orchestration/test_orchestration_group_handler.py, tests/orchestration/test_orchestration_policy_delegation.py, tests/orchestration/test_orchestration_memory_headroom.py, tests/orchestration/test_orchestration_apply.py, tests/orchestration/test_orchestration_m4_e2e.py, tests/test_apply_group_settings.py, tests/test_config.py, tests/test_spa_app_profiles.py, tests/test_group_policy.py
  > Review: three tests outside the plan's file list read the deleted mechanism and were adapted, not deleted: `test_orchestration_memory_headroom.py::test_the_ordinary_growth_saturates_the_group_when_the_container_is_full` called `_grow` directly and was removed (the contract test covers the saturation), and the checkpoint tests in `test_orchestration_apply.py` and `test_orchestration_policy_delegation.py` now hold the placement lock against `assign_user` instead of `check_occupancy`, since that is where the surviving `grow` checkpoint lives. In `test_orchestration_m4_e2e.py` the two growth steps call `start_worker()` (the stories are about stickiness, not about who births) and step 8 provokes the saturation with a refused placement. The `newcomer_reserve_count` examples became `worker_max_users` where `worker_min_life_seconds` was already taken by the same profile.
  - Pattern reference: `src/genro_asgi/spa/orchestration/group_handler.py:assign_user` (the birth inside the placement — the only birth that stays), `tests/orchestration/test_orchestration_cpu_growth.py:test_repeated_crossings_without_arrivals_never_create_workers` (the assertion style for "no worker was born")
  - Files: `src/genro_asgi/spa/orchestration/group_handler.py`, `src/genro_asgi/spa/orchestration/group_policy.py`, `src/genro_asgi/applications/spa_app.py`, `src/genro_asgi/config/handler.py`, `pyproject.toml`, `docs/guides/configuration.md`, `CLAUDE.md`, `tests/orchestration/test_orchestration_group_handler.py`, `tests/orchestration/test_orchestration_cpu_growth.py`, `tests/orchestration/test_orchestration_policy_delegation.py`, `tests/test_apply_group_settings.py`, `tests/test_config.py`, `tests/test_spa_app_profiles.py`, `tests/test_group_policy.py`
  - Decisions:
    - Delete `GroupHandler._has_room`, `GroupHandler._placeable_newcomers`, `GroupHandler._grow`. Delete the setpoint `newcomer_reserve_count` everywhere (SETPOINTS, dataclass field, property, grammar kwarg, `config/handler.py` list, docs). KEEP `new_user_occupancy_percent`: `_expected_occupancy` still reads it; the next plan removes both.
    - `check_occupancy` becomes, in this order: (1) the memory restart check, unchanged; (2) `self._judge_cpu_admission()`, unchanged; (3) if `self.living_workers` is empty → `await self.start_worker()` and return — the group must always have a reception, and this is the ONE birth without a user (it replaces what the reserve used to do for a dead-only-worker group); (4) if `self.state == "saturated"` and `self._may_grow` → `self.state = "running"`; (5) the retirement, unchanged (suspension gate, `_spare_worker`, `_order_quit`, `no_absorbable_spare_worker`). The `occupancy_reader`/`picture` computation stays because `_spare_worker` reads the picture.
    - The `saturated` state is written where the refusal happens: in `assign_user`, when `worker_handler is None` and `self._may_grow` is False and the fallback ALSO returned None, set `self.state = "saturated"` before raising `AssignmentRefused`, and move the `log_order(..., "grow", numbers={memory_occupied_percent, memory_max_percent, worker_memory_ceiling_bytes, memory_available_bytes, workers}, outcome="saturated")` from `_grow` to that spot. When the fallback DID place him the group is not saturated (a user was served).
    - `_spare_worker` loses its second question (the reserve): delete the `_placeable_newcomers(after) < policy.newcomer_reserve_count` check and the sentence about the newcomer reserve in its docstring. The other two questions stay.
    - `CHECK_OCCUPANCY_BEATS` stays (it also sizes `SHAPE_REVIEW_SECONDS`).
    - Module docstring of `group_handler.py`: the paragraphs describing the reserve and the periodic growth are rewritten to say that no worker is born without a user except the reception of an empty group. `CLAUDE.md`: in "CPU pressure gates admission; concrete demand births capacity", replace "The periodic shape judge uses memory, not CPU, while this policy is on; otherwise CPU would still pre-fork through a second road." with "The periodic judge births nothing: a group with no living worker gets its reception back, and every other birth happens inside `assign_user` for the user who needs it." `docs/guides/configuration.md` lines 378-380: drop the `newcomer_reserve_count` sentence.
    - Tests: `test_a_group_where_nobody_admits_anybody_grows_at_its_check` is REPLACED by the plan's contract test (the periodic check births nothing while a worker lives); `test_a_growth_the_quota_refuses_saturates_the_group_until_there_is_room` is rewritten so the saturation is provoked by an `assign_user` that is refused, and lifted by the next `check_occupancy` once `_may_grow` is true again; `test_a_closure_that_would_eat_the_reserve_is_not_ordered` and `test_a_worker_on_its_way_out_counts_no_room` are deleted (their mechanism is gone); `test_a_dead_only_worker_is_replaced_by_the_availability_judge` stays and must pass unchanged. In `test_apply_group_settings.py`, `test_spa_app_profiles.py`, `test_orchestration_policy_delegation.py` and `test_config.py` the `newcomer_reserve_count` examples become `worker_min_life_seconds` examples with a value different from the default (e.g. 120.0), same assertions on the same road. `test_spa_app_profiles.py:284` keeps `occupancy_max_percent: 200.0` as its invalid value and drops the second key.
  - Details:
    1. Copy `.phased/active/cpu-orchestration-cleanup/tests/phase-3/test_orchestration_no_speculative_birth.py` to `tests/orchestration/test_orchestration_no_speculative_birth.py` verbatim, and add `"tests/orchestration/test_orchestration_no_speculative_birth.py" = ["F811"]` to `[tool.ruff.lint.per-file-ignores]` in `pyproject.toml` (the fixture-import idiom the block already documents).
    2. Edit `group_handler.py` as decided; then `group_policy.py`, `spa_app.py`, `config/handler.py`.
    3. Adapt and delete the tests listed in Decisions; update `docs/guides/configuration.md` and `CLAUDE.md`.
    4. `ruff check src/ tests/`, `pytest tests/ -q`.
  - Done: `git grep -nE 'newcomer_reserve_count|_has_room|_placeable_newcomers|def _grow' -- src tests docs internals CLAUDE.md` prints nothing; `pytest tests/orchestration/test_orchestration_no_speculative_birth.py tests/orchestration/test_orchestration_group_handler.py tests/orchestration/test_orchestration_cpu_growth.py tests/test_group_policy.py -q` passes; the in-tree copy of the phase-3 contract test is byte-identical to the plan copy; `ruff check src/ tests/` zero errors; `pytest tests/ -q` has no failure that is not also failing on the parent commit.

- [x] **Phase 4**: The reception is a worker like any other
  > Done: `reception_reserved_percent` and `GroupHandler.get_worker_cap` are gone from src, tests, docs, internals and pyproject; the reception admits under the same `occupancy_max_percent` as every other worker and the census key `worker_cap` carries that setpoint. Full suite 1781 passed, `ruff check src/ tests/` clean.
  > Files: src/genro_asgi/spa/orchestration/group_policy.py, src/genro_asgi/spa/orchestration/group_handler.py, src/genro_asgi/spa/orchestration/worker_handler.py, src/genro_asgi/spa/orchestration/spa_commander.py, src/genro_asgi/applications/spa_app.py, src/genro_asgi/config/handler.py, pyproject.toml, docs/guides/configuration.md, internals/10_server/020_applications/configuration_profiles/README.md, tests/orchestration/test_orchestration_no_reception_reserve.py, tests/orchestration/test_orchestration_placement.py, tests/orchestration/test_orchestration_apply.py, tests/orchestration/test_orchestration_cpu_growth.py, tests/orchestration/test_orchestration_cpu_offload.py, tests/orchestration/test_orchestration_group_handler.py, tests/orchestration/test_orchestration_policy_delegation.py, tests/orchestration/test_orchestration_m3_e2e.py, tests/orchestration/test_orchestration_m4_e2e.py, tests/test_config.py, tests/test_group_policy.py
  > Review: the two e2e stories provoked their growth step with the reserve alone — the reception at 70% refused under a cap of 30 while a newborn of the same declared RSS admitted under 80. With one cap for everybody a worker can no longer be full while an identical newborn is empty, so the stories now make the reception HEAVY and the newborn LIGHT: the concession drops (m3 `TIGHT_CONCESSION_BYTES` 100MB to 80MB, m4 `GROWTH_CONCESSION_BYTES` the same, `FULL_CONCESSION_BYTES` 85MB to 26MB so step 8 still finds nobody admitting) and `worker_settings["worker_kwargs"]["declared_rss_bytes"]` is lowered to `SPARE_RSS_BYTES` before the birth. Every assertion of both stories is preserved; m3 reads its closed worker occupancy off `SPARE_RSS_BYTES`. `CLAUDE.md` names neither deleted thing and its `Last Updated` is already the commit date, so it is untouched.
  - Pattern reference: `library-standard` (removal of one setpoint and one method, with its readers)
  - Files: `src/genro_asgi/spa/orchestration/group_policy.py`, `src/genro_asgi/spa/orchestration/group_handler.py`, `src/genro_asgi/spa/orchestration/worker_handler.py`, `src/genro_asgi/spa/orchestration/spa_commander.py`, `src/genro_asgi/applications/spa_app.py`, `src/genro_asgi/config/handler.py`, `pyproject.toml`, `docs/guides/configuration.md`, `internals/10_server/020_applications/configuration_profiles/README.md`, `CLAUDE.md`, and every file under `tests/` that names `reception_reserved_percent` or `get_worker_cap` (`git grep -lE 'reception_reserved_percent|get_worker_cap' -- tests`; 11 files on the parent commit)
  - Decisions:
    - Delete the setpoint `reception_reserved_percent` everywhere (SETPOINTS, field, property, cross rule in `_check_cross_rules`, grammar kwarg, `config/handler.py` list, docs, the profiles README line 66).
    - Delete `GroupHandler.get_worker_cap`. Its readers: `WorkerHandler.assign_user` compares `projected` against `self.group_handler.occupancy_max_percent`; the pool census row in `SpaCommander.get_pool_census` keeps the key `"worker_cap"` with the value `group_handler.occupancy_max_percent` (the key is read by `tests/orchestration/test_orchestration_m4_e2e.py` and `test_orchestration_placement.py`; the next plan reshapes the census).
    - `GroupHandler.reception` stays: it is the routing target of anonymous and guest requests (`SpaCommander.serve_request`) and is excluded from `_spare_worker`. Nothing about admission distinguishes it any more.
    - Tests: `test_the_reception_keeps_its_reserve_and_takes_less_than_the_others` (`test_orchestration_placement.py`) is deleted; `grown_group` in `test_orchestration_cpu_growth.py` drops its `setdefault("reception_reserved_percent", 0.0)`; every profile literal `"reception_reserved_percent": 0.0` in `test_orchestration_apply.py`, `test_orchestration_cpu_offload.py`, `test_orchestration_group_handler.py`, `test_orchestration_m3_e2e.py`, `test_orchestration_m4_e2e.py`, `test_orchestration_policy_delegation.py`, `test_config.py`, `test_group_policy.py` is removed; a test whose ONLY point was the reception's reserve is deleted, a test that used the reserve as setup keeps its assertions. `test_group_policy.py` tests of the reception cross rule are deleted.
    - `CLAUDE.md`: no paragraph names `reception_reserved_percent`; nothing to change there beyond `Last Updated`. `docs/guides/configuration.md` line 375: drop the sentence on `reception_reserved_percent`.
  - Details:
    1. Copy `.phased/active/cpu-orchestration-cleanup/tests/phase-4/test_orchestration_no_reception_reserve.py` to `tests/orchestration/test_orchestration_no_reception_reserve.py` verbatim, and add `"tests/orchestration/test_orchestration_no_reception_reserve.py" = ["F811"]` to `[tool.ruff.lint.per-file-ignores]` in `pyproject.toml`.
    2. Edit `group_policy.py`, `group_handler.py`, `worker_handler.py`, `spa_commander.py`, `spa_app.py`, `config/handler.py`, the two docs.
    3. Adapt the tests as decided.
    4. `ruff check src/ tests/`, `pytest tests/ -q`.
  - Done: `git grep -nE 'reception_reserved_percent|get_worker_cap' -- src tests docs internals CLAUDE.md` prints nothing; `pytest tests/orchestration/test_orchestration_no_reception_reserve.py tests/test_group_policy.py tests/orchestration/test_orchestration_placement.py tests/orchestration/test_orchestration_apply.py -q` passes; the in-tree copy of the phase-4 contract test is byte-identical to the plan copy; `ruff check src/ tests/` zero errors; `pytest tests/ -q` has no failure that is not also failing on the parent commit.

- [ ] **Phase 5**: Coherence review and auto-fix (final, mandatory)
  - Pattern reference: same as Phases 1..4 (cross-check against them)
  - Files: only the files written by Phases 1..4 (collect them from their `Files:` fields). Never touch a pre-existing file they did not modify.
  - Decisions:
    - Auto-fix directly: tool-fixable lint (ruff), unused imports, formatting, trivially mechanical fixes. Re-run the tests after each non-tooling fix; if one breaks a test, roll back that fix and flag it instead.
    - Never auto-fix: logic errors, design divergences from the pattern reference, missing edge cases, anything architectural. Those go to `review.md` only.
    - Report in `review.md` every docstring or `CLAUDE.md` sentence that still describes the reserve, the periodic growth, the reception's reserve, `/proc` or the old threshold names.
  - Details: convergence loop (max 3 cycles) of `ruff check` scoped to the file set → auto-fix → `ruff check` → `pytest tests/ -q`; stop early if a cycle makes no progress. Then write `.phased/active/cpu-orchestration-cleanup/review.md` with three sections: **Auto-fixed** (file, what, tool), **Flagged for human** (file, description, suggested action), **Final state** (linter output, suite result, files reviewed).
  - Done: `review.md` exists in the plan directory with the three sections, `ruff check` zero errors on the file set, `pytest tests/ -q` has no failure that is not also failing on the parent commit.

## Notes
- Base of the workflow: `feat/cpu-orchestration` at `36241da` (the commit that measured `temp4`). One test fails there on macOS (`tests/orchestration/test_orchestration_apply.py::test_no_admission_window_on_apply`); every `Done:` compares the suite against that baseline, never against "all green".
- `pip install <name>` is denied in auto mode; Phase 1 installs psutil through the manifest only.
- The next plan (interactive, in-chat) writes the asymmetric filter inside `record_cpu_reading`, the new gate in `WorkerHandler.assign_user`, the admission interval, the memory veto, the removal of the user cost estimate and the CPU retirement. Nothing here anticipates it: see `Must not break:`.
- Decision register of the whole rework: `temp/scheda_ragioni_cpu_orchestration_2026-09-01.md` (untracked, in this worktree).

## Suggested execution config
| Phase | Effort | Model |
|-------|--------|-------|
| Phase 1 | medium | opus |
| Phase 2 | medium | opus |
| Phase 3 | medium | opus |
| Phase 4 | medium | opus |
| Phase 5 | xhigh | opus |
