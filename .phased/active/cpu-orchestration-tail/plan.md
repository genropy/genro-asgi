# Context: wf/cpu-orchestration-tail
Parent: feat/cpu-orchestration
Mode: autonomous
Channel: relayed
Must not break: `WorkerHandler.record_cpu_reading` stays the ONLY writer of `cpu_temperature_percent`; the photo gains no CPU reader
Must not break: every journal reason code of the parent keeps its spelling (`cpu_temperature_missing`, `no_absorbable_spare_worker`, `cpu_pressure_holds_retirement`, the placement codes); this plan changes WHEN a row is written, never its name
Must not break: the worker photo keeps every non-CPU field (`rss_bytes`, `pss_bytes`, the user rows with their service counters); only `cpu_seconds` and the derived `cpu_percent` leave

## Objective
The tail of the CPU orchestration rework: remove what the two previous plans left without readers, and make the retirement journal say one thing per round. The photo's CPU smoothing (`cpu_seconds` → `cpu_percent`) has no reader since the commander-side thermometer became the only CPU input; two placement tests lost their second racer when the periodic growth was removed and now pass by construction; a round suspended by a missing temperature is journaled twice.

## Work Plan
- [x] **Phase 1**: The photo carries no CPU any more
  > Done: `cpu_seconds`/`cpu_percent` removed from `spa_worker.worker_snapshot` and from `WorkerEnvelopeHandler.on_worker_snapshot` (with `CPU_SMOOTHING_FACTOR`, `CPU_WINDOW_MIN_SECONDS`, `_last_cpu`, `_cpu_percent` and the now-unused `import time`); the four CPU tests of the envelope chain deleted and the five other test files cleaned of the photo's CPU keys; `CLAUDE.md` reworded. `pytest tests/ -q` 1786 passed; `ruff check src/ tests/` zero errors.
  > Review: the Done grep still prints three lines the criterion did not enumerate — `worker_handler.py:428/430/440` and `test_orchestration_cpu_temperature_meter.py:105/109` — all the commander-side thermometer (`record_cpu_reading`'s own local `cpu_seconds` and the psutil double feeding it), the same category the criterion accepts by name for `test_orchestration_cpu_meter_psutil.py`. Nothing of the photo's CPU is left.
  > Files: `src/genro_asgi/spa/orchestration/envelope_handler.py`, `src/genro_asgi/spa/orchestration/spa_worker.py`, `CLAUDE.md`, `tests/orchestration/test_orchestration_envelope_chain.py`, `tests/orchestration/test_orchestration_cpu_temperature_meter.py`, `tests/orchestration/test_orchestration_group_handler.py`, `tests/orchestration/test_orchestration_pss_accounting.py`, `tests/orchestration/test_orchestration_spa_worker_process.py`, `tests/orchestration/test_orchestration_user_service_counters.py`
  - Pattern reference: `src/genro_asgi/spa/orchestration/envelope_handler.py:_derive_user_service` (the per-photo derivation that STAYS — the CPU one beside it goes), `tests/orchestration/test_orchestration_envelope_chain.py:test_the_photo_is_filed_by_the_bottom_layer_and_may_wait_for_its_round` (the test style for the fold)
  - Files: `src/genro_asgi/spa/orchestration/envelope_handler.py`, `src/genro_asgi/spa/orchestration/spa_worker.py`, `CLAUDE.md`, `tests/orchestration/test_orchestration_envelope_chain.py`, `tests/orchestration/test_orchestration_cpu_temperature_meter.py`, `tests/orchestration/test_orchestration_group_handler.py`, `tests/orchestration/test_orchestration_pss_accounting.py`, `tests/orchestration/test_orchestration_spa_worker_process.py`, `tests/orchestration/test_orchestration_user_service_counters.py`
  - Decisions:
    - Delete from `envelope_handler.py`: the constants `CPU_SMOOTHING_FACTOR` and `CPU_WINDOW_MIN_SECONDS` (with their `#:` comments and any `__all__` entry), the attributes `self._last_cpu` and `self._cpu_percent` in `WorkerEnvelopeHandler.__init__`, and the whole CPU block of `on_worker_snapshot` (from `cpu_seconds = photo.get("cpu_seconds")` to `photo["cpu_percent"] = self._cpu_percent` inclusive). `on_worker_snapshot` keeps `self._derive_user_service(photo)` and the filing of the photo. Its docstring loses the two CPU paragraphs and keeps the user-rows one; drop the sentence "like the first CPU photo". Delete `import time` if nothing else in the module uses it.
    - Delete from `spa_worker.py`: the property `cpu_seconds` and the key `"cpu_seconds": self.cpu_seconds,` in `worker_snapshot`; in the `worker_snapshot` docstring the sentence that says `cpu_seconds` becomes `cpu_percent` at the vertex is rewritten to say the CPU is measured by the commander through psutil, never by the photo. Delete `import os` only if nothing else uses it (check with `git grep -n 'os\.' src/genro_asgi/spa/orchestration/spa_worker.py`).
    - `CLAUDE.md`: in the paragraph "The photo counts each user's service", replace "the same road ``cpu_seconds`` takes to ``cpu_percent``" with "derived between two photos by the fold"; `Last Updated` follows.
    - Tests: delete the four CPU tests of `test_orchestration_envelope_chain.py` (`test_two_photos_apart_the_cpu_seconds_become_a_share_of_one_core`, `test_a_cpu_burning_more_than_one_core_reads_as_one`, `test_a_sustained_load_converges_on_the_true_share`, `test_photos_inside_the_minimum_window_carry_the_standing_value`) and any helper only they used. In `test_orchestration_cpu_temperature_meter.py` the dummy photos `{"cpu_percent": 7.0}` / `11.0` / `90.0` become `{"rss_bytes": 7}` / `11` / `90` (a photo that is a photo, with a field the fold does not derive), and the test `test_a_full_photo_and_the_temperature_remain_independent` asserts on `rss_bytes` instead; `test_orchestration_group_handler.py:400` and `:638` drop the `cpu_percent` key (the second is inside a photo literal — remove the line); `test_orchestration_pss_accounting.py:96` drops the key; `test_orchestration_spa_worker_process.py:364` drops the `photo.pop("cpu_seconds")` assertion; `test_orchestration_user_service_counters.py:23` rewords its module docstring ("the deltas between two photos" without the `cpu_seconds` comparison).
  - Details: envelope_handler first, then spa_worker, then the tests in the order listed, then `CLAUDE.md`; `ruff check src/ tests/`; `pytest tests/ -q`.
  - Done: `git grep -nE 'CPU_SMOOTHING_FACTOR|CPU_WINDOW_MIN_SECONDS|_last_cpu|_cpu_percent|cpu_seconds|"cpu_percent"' -- src tests CLAUDE.md docs internals` prints nothing except `tests/orchestration/test_orchestration_cpu_meter_psutil.py` (whose test NAME says `cpu_seconds` and whose psutil double returns cpu seconds — that is the thermometer, not the photo); `pytest tests/ -q` green; `ruff check src/ tests/` zero errors.

- [ ] **Phase 2**: One journal row per suspended retirement, and no test that passes by construction
  - Pattern reference: `src/genro_asgi/spa/orchestration/group_handler.py:check_occupancy` (the round that owns the journal rows of the retirement), `tests/orchestration/test_orchestration_cpu_growth.py:test_a_worker_with_no_temperature_suspends_the_retirement` (the test that reads the row by reason)
  - Files: `src/genro_asgi/spa/orchestration/group_handler.py`, `tests/orchestration/test_orchestration_cpu_growth.py`, `CLAUDE.md`
  - Decisions:
    - The missing-temperature check moves OUT of `_spare_worker` INTO `check_occupancy`, right before the retirement suspension gate: `missing = sorted(w.name for w in self.living_workers if w.get_cpu_temperature_percent() is None)`; when non-empty, `log_decision(self.name, "retirement", "no_action", reason="cpu_temperature_missing", numbers={"workers": len(self.living_workers), "missing": missing})` and `return` — so neither the suspension row nor `no_absorbable_spare_worker` follows it. `_spare_worker(policy)` then reads `worker_handler.get_cpu_temperature_percent()` for every living worker as a `float` (no None branch left) and its docstring drops the "cpu_temperature_missing" sentence: the caller guarantees the temperatures.
    - `tests/orchestration/test_orchestration_cpu_growth.py`: `test_a_worker_with_no_temperature_suspends_the_retirement` asserts that the retirement rows of that round are EXACTLY one, with reason `cpu_temperature_missing` (replace the "read by reason" form with `rows = [d for d in decisions if d["decision"] == "retirement"]; assert [r["reason"] for r in rows] == ["cpu_temperature_missing"]`). Delete `test_a_crossing_and_a_placement_race_to_one_spawn` and `test_the_reactive_growth_and_a_placement_cannot_fork_twice`: both raced `check_occupancy` against a placement to prove one spawn, and `check_occupancy` births nothing since the periodic growth was removed, so `len(worker_handler_map) == 2` is true by construction. `test_concurrent_arrivals_share_one_demand_born_worker` (two placements racing) stays: that race still exists.
    - `CLAUDE.md`: in the retirement paragraph, "a living worker with no temperature yet is journaled `cpu_temperature_missing` and nothing closes" stays true; add "— one row, and no other retirement row that round". `Last Updated` follows.
  - Details: `check_occupancy` first, then `_spare_worker`, then the tests, then `CLAUDE.md`; `ruff check src/ tests/`; `pytest tests/orchestration/test_orchestration_cpu_growth.py tests/orchestration/test_orchestration_group_handler.py -q`; `pytest tests/ -q`.
  - Done: `git grep -n cpu_temperature_missing -- src` prints exactly one line, inside `check_occupancy`; `git grep -nE 'def test_a_crossing_and_a_placement_race_to_one_spawn|def test_the_reactive_growth_and_a_placement_cannot_fork_twice' -- tests` prints nothing; `pytest tests/ -q` green; `ruff check src/ tests/` zero errors.

- [ ] **Phase 3**: Coherence review and auto-fix (final, mandatory)
  - Pattern reference: same as Phases 1..2 (cross-check against them)
  - Files: only the files written by Phases 1..2 (collect them from their `Files:` fields). Never touch a pre-existing file they did not modify.
  - Decisions:
    - Auto-fix directly: tool-fixable lint (ruff), unused imports, formatting, trivially mechanical fixes. Re-run the tests after each non-tooling fix; if one breaks a test, roll back that fix and flag it instead.
    - Never auto-fix: logic errors, design divergences from the pattern reference, missing edge cases, anything architectural. Those go to `review.md` only.
    - Report in `review.md` every docstring or `CLAUDE.md` sentence that still describes the photo's CPU smoothing or a retirement row this plan removed.
  - Details: convergence loop (max 3 cycles) of `ruff check` scoped to the file set → auto-fix → `ruff check` → `pytest tests/ -q`; stop early if a cycle makes no progress. Then write `.phased/active/cpu-orchestration-tail/review.md` with three sections: **Auto-fixed** (file, what, tool), **Flagged for human** (file, description, suggested action), **Final state** (linter output, suite result, files reviewed).
  - Done: `review.md` exists in the plan directory with the three sections, `ruff check` zero errors on the file set, `pytest tests/ -q` green.

## Notes
- Base: `feat/cpu-orchestration` at `2d2a50c` (plans A and B consolidated). Suite green there (1790), so every `Done:` asks for green, not for baseline attribution.
- Out of scope, deliberately: the bulk reclassification of the orchestration tests into contract vs implementation (rule 10 of the repo) — the owner's own task, never smuggled into another change; the memory gauges through psutil; the bench in genropy-asgi.
- Decision register of the whole rework: `temp/scheda_ragioni_cpu_orchestration_2026-09-01.md` (untracked, this worktree).

## Suggested execution config
| Phase | Effort | Model |
|-------|--------|-------|
| Phase 1 | medium | opus |
| Phase 2 | medium | opus |
| Phase 3 | xhigh | opus |
