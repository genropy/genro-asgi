✓ Phase 6 completed: Coherence review and auto-fix — commit `94de67a`, tree clean, suite 1569 passed / 2 skipped, `ruff check .` clean.

Three checks ran for real: 641 `file:line` references resolve against the current tree (0 unresolved), coverage holds (59/59 zone cards land in a register or Scartate, 46 verdict slots all still empty), and a semantic anchor check over 185 symbol+anchor pairs flagged 4 — all four correct on inspection. Nine auto-fixes, every one the same species: a numeral contradicting its own tree-verified anchor list (three caller counts stated in two files each, one card count, two form slips); `review.md` records them plus four items flagged for the owner, including the 11 never-carded ebook claims left as a perimeter call rather than invented as findings. This was the last phase — the plan is ready for `/finalize-workflow`.
